# MAVLab v1.5.3 In-Place Update Validation Record

Release: existing `v1.5.3` release, updated in place
Validated source commit: `e7b9b301a26a2478e11fa7e6877fb8589c92192c`
Date: 2026-09-02
Tester: Ascend Labs with Codex-assisted execution

## Scope

- Keep motor audio active while the simulation runtime is retained for QGroundControl.
- Keep phone-sensor input disabled while MAVLab is backgrounded.
- Release audio, sensors, networking, simulation, and power locks when the runtime stops.
- Increase the same-phone QGroundControl handoff window from 45 seconds to one minute.
- Update the existing v1.5.3 release notes and assets without creating a new release.

## Candidate artifacts

| Artifact | Size | SHA-256 |
|---|---:|---|
| `MAVlab.apk` | 53,309,637 bytes | `4a81e6a9ff67043e778ba42c3ae111774bbe85edafea5f5a73d9dc49c0df107d` |
| `app-release.aab` | 33,289,051 bytes | `ec56d3cb8a02dbce6592c457b5317e097af017b4c1001a5a630b58b56e9aba81` |

Application metadata remained:

- application ID: `com.ascend.mavlab`
- version name: `1.5.3`
- version code: `18`
- minimum SDK: `26`
- target SDK: `35`

## Automated verification

Command:

```bash
cd mavlab-android
GRADLE_USER_HOME="$PWD/.gradle" ./gradlew lintDebug testDebugUnitTest assembleDebug bundleRelease --console=plain
```

Result: `BUILD SUCCESSFUL` in 1m 35s.

- Android lint: passed
- debug APK assembly: passed
- release AAB bundle: passed
- unit tests: 155 tests, 0 failures, 0 errors, 0 skipped
- focused runtime-component policy: background runtime disables phone sensors and retains motor audio
- focused handoff boundary: retained at exactly 60,000 ms and idle-stop eligible after 60,000 ms
- staged whitespace check: passed

## Device and QGroundControl evidence

Device: Samsung SM-G970U
Android: 12
GCS: QGroundControl, same-phone loopback

Observed on 2026-09-02:

1. The final debug APK installed successfully in place with application data retained.
2. Android reported MAVLab `versionName=1.5.3`, `versionCode=18`, after installation.
3. With QGroundControl foreground and MAVLab backgrounded, `SimulationService` remained active.
4. Android AudioFlinger reported an active audio track owned by the MAVLab process while QGroundControl was foreground.
5. The one-minute boundary is covered by deterministic policy tests; a full 60-second physical idle-timeout observation was not repeated after the final installation.

## Publication contract

The `v1.5.3` tag is intentionally moved to the validated update after publication. The Android release workflow now detects an existing GitHub release, updates its notes, and replaces the APK/AAB using `--clobber`. It creates a release only when the tag has no existing release.
