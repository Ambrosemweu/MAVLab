package com.ascend.mavlab.feature.controller

enum class ControllerInputMode(val label: String) {
    PHONE_SENSORS("Phone sensors"),
    CUSTOM_INPUT("Custom input"),
    DIRECT_RPM("Direct RPM"),
}
